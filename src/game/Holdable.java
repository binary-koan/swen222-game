package game;

/**
 * Indicates an instance of and item that can be picked up.
 * @author holdawscot
 *
 */
public class Holdable extends Item {
    public Holdable(String id, String name, String description, String spriteName) {
        super(id, name, description, spriteName);
    }
}
